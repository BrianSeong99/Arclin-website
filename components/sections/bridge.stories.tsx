import type { Meta, StoryObj } from "@storybook/nextjs-vite";
import { Bridge } from "./bridge";

const meta: Meta<typeof Bridge> = { title: "Sections/Bridge", component: Bridge };
export default meta;
export const Default: StoryObj<typeof Bridge> = {};
